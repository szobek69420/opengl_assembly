#version 330 core
layout (location=0) in vec3 aPos;
layout (location=1) in vec3 aColour;

out vec3 colour;

uniform mat4 pv;
uniform mat4 model;

void main()
{
	colour=aColour;
	gl_Position=pv*model*vec4(aPos, 1.0);
}